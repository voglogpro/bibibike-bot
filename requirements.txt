aiogram==3.13.1
aiohttp==3.10.11
aiosqlite==0.20.0
python-dotenv==1.0.1
pytz==2024.1
cryptography==43.0.3
# ЭКСПЕРИМЕНТ: распознавание скриншотов техприложения.
# Если библиотеки не установятся — бот запустится и будет работать
# как раньше, эксперимент просто не активируется.
Pillow==10.4.0
numpy==1.26.4
# pytesseract необязателен: основной путь — собственные эталоны цифр.
# Оставлен как запасной вариант, если на сервере есть движок tesseract.
pytesseract==0.3.13
